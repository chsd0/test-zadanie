import styles from './header.module.scss';

const Header = () => (
      <header className={styles.header}>
        <span className={styles.header__title}>Инфраструктура предприятия</span>
      </header>
);

export default Header;